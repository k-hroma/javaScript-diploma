// Convertir de numero a cadena
let a = 10, b = 20;
// Si son valores numericos se suman
console.log(a + b);
// Convertir numero a cadena
let contacatenacion = a.toString() + b.toString();
//los parentesis van porque es un método asociado a la clase (si fuese un atributo no llevaría paréntesis)
console.log(contacatenacion);