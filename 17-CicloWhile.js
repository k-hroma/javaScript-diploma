// Ciclo while
// Imprimir los valores del 0 al 5
//Definimos la variable contador que irá incrementado (++) en cada iteración
let contador = 0, repeticiones = 5;
while(contador <= repeticiones){
    console.log(contador++);
    //la forma simplificada del código:
    //console.log(contador)
    // contador++ 
}
