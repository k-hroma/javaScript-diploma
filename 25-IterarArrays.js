// Arreglos en JS

// Declaracion del arreglo
let numerosArreglo = [13, 21, 0, 0, 62];

// Leer los valores
console.log(numerosArreglo.length);
for (let i = 0; i < numerosArreglo.length; i++) {
  console.log(`Arreglo[${i}] = ${numerosArreglo[i]}`);
}

// forEach:

// Es un método específico de los arrays que toma una función como argumento y ejecuta esa función una vez por cada elemento del array.
// Ejemplo
array.forEach((element) => {
  console.log(element);
});
