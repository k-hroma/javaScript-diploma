// Constantes en JavaScript
//es una variable que una vez definido su valor, ya no se puede modificar
//como buena práctica, el nombre de una constante se define en mayúsculas, y si tiene más palabras se separan con guión bajo.

//Ejemplo:

const MI_CONSTANTE = 10;

// MI_CONSTANTE = 20 //da error

console.log(MI_CONSTANTE)
console.log(Math.PI) // hay constante ya definidas (Math es una clase de JS )

// -> no se puede declarar una cte y luego incializarla, hay que hacerlo en la misma línea: el ejemplo siguiente da error
// const MI_CONSTANTE_2
// MI_CONSTANTE_2 = 20 