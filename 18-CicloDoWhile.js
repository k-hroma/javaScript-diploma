// Ciclo do while: ejecuta al menos una vez su bloque de código debido a que la condición a revisar se encuentra al final del bloque
// Imprimir los valores del 0 al 5
let contador = 0, repeticiones = 5;
do{
    console.log(contador++);
    //contador++;
}while(contador <= repeticiones);
